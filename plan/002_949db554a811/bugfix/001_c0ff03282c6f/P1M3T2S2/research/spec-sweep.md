# spec/ living-documents sweep — P1.M3.T2.S2 (Bugfix Wave 1)

Sweep record: one row per contract target (navigate-by-quoted-text; pre-wave line
numbers were stale). Statuses: **verified-as-is** (ridden Mode A commits already
carried the fixed semantics — confirmed against src/ ground truth) /
**corrected** / **gap-filled**. All edits trace to h2.5 Recommendations +
BUG-001..008 final semantics. No pin deleted; no file restructured; no src/ or
README.md changes.

## Per-target results

| # | File · target | Verdict | Detail (quote → action) | BUG | Ground truth |
|---|---|---|---|---|---|
| 1 | product-requirements.md FR-3 gate-hold | verified-as-is | "while gate-group questions are unanswered, commits show the non-expiring `⚠ …` line instead of auto-submitting; `ctrl+s` overrides" — commit-scoped, every entry point (option accept, Other write-in commit, text-answer enter, edits) already enumerated in FR-3's auto-submit clause | BUG-001 | src/panel/actions.ts maybeAutoSubmit tail (gate check on every commit) |
| 2 | product-requirements.md FR-12 free-text duties | gap-filled | Added after the two-stage-removal sentence: "The duty is re-derived from the cursor position on every question change (BUG-004) — `enter` after navigation follows the fresh cursor, never the stale entry duty." (write-through invariant already lives in ESC-002 + ui-spec :62 "Every exit is a draft write-through"; FR-12's "exiting the field never suspends" unchanged) | BUG-004 | src/panel/panel.ts:1041–1129 (syncBufferToQuestion tail re-derives textDuty via desiredTextDuty) |
| 3 | product-requirements.md FR-32 bridge | verified-as-is | "An unexpected internal error mid-pipeline emits an `internal_error` submit-result nack and completes the flow — the client is always acked … next upsert resurface heals the surface (BUG-007, D-R6)" — present, unhedged | BUG-007 | src/remote-submit.ts (nack at both pipeline exits) |
| 4 | product-requirements.md AC-13 (:91) | verified-as-is | "13. Editing an archived answer re-marks it pending; next submission diff card highlights the change (`Q3: sqlite → postgres (changed)`)." — no hedge, no "best-effort" | BUG-003 | src/lifecycle.ts:225–232 (close-pass snapshot supplies `before.status === "closed"`) + src/snapshots.ts editedArchived |
| 5 | product-requirements.md AC-2 (:76) + AC-2c | gap-filled | AC-2 item 2 already says "≤3-line delta + reminder" ✓; 2c text-enter flow ✓. FR-3(a) said only "~2 lines + reminder …" with no shape guarantee → appended: "the ≤3-line shape is enforced — write-in/text entries flattened to one line and per-entry capped, BUG-006" | BUG-006 | src/snapshots.ts:136 `flatten` (single-line guarantee) + src/delivery.ts budget cap |
| 6 | ui-spec.md Other row (:13/:52) + duties (:53) | gap-filled ×2 | (a) :52 appended revisit visibility: "Recorded write-ins stay visible on revisit: the question line's `✎` marker covers `answer.custom` (parity with text answers — BUG-005) and a dimmed first-line preview of `answer.value` renders under the Other row." (b) :53 appended: "— and is re-derived from the new cursor position on every question change (BUG-004 …)" | BUG-005, BUG-004 | src/panel/layout.ts:186–194 (hasTextAnswer `custom === true`) + src/panel/short-view.ts answerPreviewLine (custom → `answer.value` first line) + panel.ts:1041–1129 |
| 7 | ui-spec.md :72 gate-hold aside | corrected | Old: "but note the completeness rule already covers the common case (…nothing would fire). The observable gate-hold behavior is the EXPLANATION…" — implied the ⚠ line only matters in cases the completeness rule misses (the exact premise BUG-001 disproved). New: "The common case is exactly what the hold line exists FOR: … the ⚠ line is what makes that hold visible instead of silent (BUG-001: it arms on EVERY commit while gate questions are unanswered, including commits the completeness check already withholds)." Hold-line string unchanged | BUG-001 | h2.5 rec 1; src/panel/actions.ts gate-hold arm on every commit |
| 8 | ui-spec.md :129 renderer + §Remote bridge | gap-filled (renderer side verified-by-other-file) | Renderer :129 already renders `{title}: {old} → {new (changed)}` + `✎ {text}` truncated — reachability premise owned by state-and-persistence.md :124 (defer convention). §Remote bridge (:41) documented `flow_not_found` + foreign silence but NO internal_error nack → appended: "An unexpected internal error in the submit pipeline nacks `internal_error` and completes the flow — the client is always acked (ok or nack), never hangs; no rollback, the next upsert resurface heals the surface (BUG-007, D-R6)." (mirrors FR-32/D-R6/architecture.md wording) | BUG-007, BUG-003 | src/remote-submit.ts; ui-spec renderer unchanged |
| 9 | decisions.md :13 ESC-002 + CTRL-C-001 (:17) | gap-filled (CTRL-C pin) | ESC-002 generic invariant "All exits are draft write-throughs" ✓ covers esc-esc/ctrl+t. CTRL-C-001 had no draft note → appended: "The suspend is itself a write-through: the in-flight editor draft stages to its slot before `done(null)` (R4 — BUG-002)." (ui-spec :62 independently carries "Every exit is a draft write-through") | BUG-002 | h2.5 rec 2 (write-through helper at top of suspend()/discussInChat/enterNoteMode) |
| 10 | decisions.md :21 WRITEIN-001 cursor-follow | corrected (pin amended, not deleted) | "the duty follows entry path and cursor position." → "…cursor position (re-derived on every question change — BUG-004)." — trailing-parenthetical amendment per pin convention | BUG-004 | src/panel/panel.ts:1041–1129 |
| 11 | decisions.md :27 R4 hard-requirement line | verified-as-is | "R4 draft preservation (from ask_user evaluation: state loss on revisit is disqualifying)" — consistent with every-exit write-through (the invariant sentences in ESC-002/CTRL-C-001/ui-spec :62/:63 operationalize it; no contradiction) | BUG-002 | — |
| 12 | decisions.md :40–41 D-R5/D-R6 | verified-as-is | D-R6 carries "An unexpected internal error in the submit pipeline nacks `internal_error` … (BUG-007)" ✓. D-R5 step list (baseline→diff→BUG-008 filter→markSubmitted→buildSubmission→deliverSubmission→noteSubmissionDelivered) matches remote-submit.ts's documented pipeline order ✓ (maybeAutoSubmit tail documented at the architecture/ui-spec level, not duplicated here — defer convention) | BUG-007, BUG-008, BUG-006 | src/remote-submit.ts:21–52 header contract |
| 13 | architecture.md :149–162 bridge submit flow | gap-filled | Existing diagram matched remote-submit.ts (parse → validate → applyAnswer → baseline/diff/pendingIds → BUG-008 filter → markSubmitted → buildSubmission [snapshot+bump once] → deliverSubmission → noteSubmissionDelivered; internal throw → internal_error nack + completed) but omitted the auto-submit tail → inserted diagram line: "→ (this submit completes the set → maybeAutoSubmit tail — same auto-submit + gate-hold semantics as the panel, AUTOSUBMIT-001 bridge parity)" | BUG-007, BUG-008 | src/remote-submit.ts:112–115 (tail at BOTH exits, after noteSubmissionDelivered) |
| 14 | state-and-persistence.md :124 snapshot invariant | verified-as-is | "every close pass that actually closes ids appends ONE snapshot at the submission's existing epoch — no epoch bump — whose question statuses are `closed` … digestSince treats them as no-ops: consecutive same-epoch pairs whose answer signatures are identical contribute zero segments" — matches lifecycle.ts + snapshots.ts exactly | BUG-003 | src/lifecycle.ts:205–232; src/snapshots.ts:339–350 ("SAME-EPOCH / STATUS-ONLY LINKS") |
| 15 | tool-protocol.md submit-result error values | verified-by-other-file | tool-protocol.md is agent-facing (tool schema + description + epoch param); submit-results go to bridge clients, never the model — importing bridge detail would break the file's scope discipline. The value set is specified authoritatively elsewhere: `flow_not_found` (ui-spec :41), `invalid_answer` (D-R4), `internal_error` (FR-32 / D-R6 / architecture.md :159 / ui-spec :41 post-gap-fill) | BUG-007 | spec/ui-spec.md, spec/decisions.md D-R4/D-R6 |

## Cross-file contradiction sweep (Level 3 grep evidence)

All greps run at final state; every hit pair states the same rule:

- `grep -rn "foundational unanswered" spec/` → PR FR-3 (:27), AC-2d (:80), ui-spec :21 (submit-time *dismissible* warning — deliberately different message, `later answers may shift`), ui-spec :72 (the non-expiring hold line). Hold-line wording identical between FR-3 / AC-2d / ui-spec :72. **CLEAN**
- `grep -rn "internal_error" spec/` → FR-32, D-R6, architecture.md :159, **ui-spec :41 (gap closed this sweep)** — all four: nack + completed + no rollback + resurface heals. **CLEAN**
- `grep -rn "(changed)" spec/` → AC-13 (:91), ui-spec renderer (:129), state-and-persistence :26/:124 — no hedge anywhere; decisions :33 hit is the unrelated O(changed) edit-cost rationale. **CLEAN**
- `grep -rn "write-through" spec/` → ESC-002 (generic invariant), CTRL-C-001 (suspend write-through — this sweep), ui-spec :62 ("Every exit is a draft write-through — backing out never loses typed text (R4)") + :63 (per-question buffer write-through on navigation). No exit described as discarding. **CLEAN**
- `grep -rn "two-stage" spec/` → every hit is a historical pin (Q17), a REMOVED note (WRITEIN-001, ui-spec :53/:58, FR-12), or plan instructions saying REMOVE (implementation-plan). No live two-stage behavior. **CLEAN**
- `grep -rn "flow_not_found\|invalid_answer" spec/` → ui-spec :41 (flow_not_found + cross-talk), D-R4 (invalid_answer + re-emit), FR-33 (cancel). Value set disjoint and consistent; each file owns its surface. **CLEAN**
- `grep -rn "cursor position" spec/` → WRITEIN-001, FR-12, ui-spec :53 — all three now state follow + re-derivation on question change. **CLEAN**
- `grep -rn "≤3-line\|~2 lines\|3-line" spec/` → SPEC.md :10 ("~2 lines" digest restatement — approximation, no contradiction), FR-3(a) ("~2 lines … ≤3-line shape is enforced — flattened + capped, BUG-006"), AC-2 item 2 ("≤3-line delta"). **CLEAN**

## Gates

- `npm run typecheck` → clean (zero source touched).
- `npm test` → 1278 passed.
- `git diff --stat` → spec/product-requirements.md, spec/ui-spec.md, spec/decisions.md,
  spec/architecture.md (+ plan/**/tasks.json and README.md per the orchestrator /
  parallel S1 task — no src/ changes).
- decisions.md pins: none deleted; two amendments are appended parentheticals
  (CTRL-C-001, WRITEIN-001) per the ledger's existing "(BUG-007)" convention.
